# Python Built-In Modules

"""
NOTES:
-------
- A Python module may be referred to as a standard library in other languages.
- help(package_name) returns package's documentation
- print(dir(package_name)) returns all methods in the package.

"""

import random

my_list = [1, 2, 3, 4, 5]
random.shuffle(my_list)
print(my_list)